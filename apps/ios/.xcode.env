export NODE_BINARY=$(command -v node)
# React Native is hoisted to the monorepo root (pnpm), not the RN JS workspace.
# Xcode build-phase scripts look up tool paths from $REACT_NATIVE_PATH, so point
# it at the real on-disk location.
export REACT_NATIVE_PATH="$PODS_ROOT/../../../node_modules/react-native"
