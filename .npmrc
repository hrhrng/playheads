# Force flat node_modules at the workspace root so apps/ios's Podfile can
# resolve react-native at ../../node_modules/react-native. pnpm's default
# (isolated) symlink layout also tickles a CocoaPods bug ("pathname contains
# null byte" on `pod install`, CocoaPods #12866).
node-linker=hoisted
